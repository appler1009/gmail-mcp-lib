{
  "name": "gmail-mcp-lib",
  "version": "1.0.0",
  "description": "TypeScript npm package providing Gmail access via official SDK with dual mode: standalone MCP stdio server and library for Node.js/TS projects",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "bin": {
    "gmail-mcp": "dist/server.js"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "jest",
    "test:coverage": "jest --coverage",
    "prepublishOnly": "npm run build"
  },
  "keywords": [
    "gmail",
    "mcp",
    "model-context-protocol",
    "typescript"
  ],
  "author": "@appler1009",
  "license": "MIT",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^0.7.0",
    "google-auth-library": "^9.6.0",
    "googleapis": "^139.0.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@types/jest": "^29.5.12",
    "@types/node": "^20.11.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.2",
    "typescript": "^5.3.3"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
